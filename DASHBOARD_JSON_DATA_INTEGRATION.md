# 主页 JSON 数据契约与合作对接说明

## 1. 文档用途

本文是本中控屏项目与模拟器、后端、设备控制层合作时的数据交接契约。
它回答四个问题：

1. 哪一方拥有哪份数据，谁可以读、谁可以写；
2. `dashboard_settings.json` 和 `dashboard_runtime.json` 的字段、类型、单位和约束；
3. 应用启动、2 秒轮询、用户控制和未来真实设备接入时，数据怎样流动；
4. 文件异常、写入失败、设备超时和版本升级时，合作方应该看到什么结果。

本文描述的是当前代码已经落地的文件边界，以及未来真实设备接入时必须保持的分层策略。
RS485、网络协议、设备地址和具体后端实现不在本文定义；合作方只要保证下文的文件契约和结果语义，
底层可以自行选择实现方式。

## 1.1 适用范围与明确边界

当前方案的产品前提是：**一个中控屏项目、一个主控应用、一个本地数据目录**。
两份 JSON 是这个主控应用与模拟器/设备层之间的轻量数据交换和持久化联调层，目标是让主页、
历史、设置等页面在中控屏上可靠地读到数据，并让控制操作有明确的写入和读回结果。

它适合以下场景：

- 单个中控屏主控应用的启动读档；
- 传感器、天气、告警等运行快照的低频刷新；
- 用户点击设备开关后的控制确认；
- macOS 联调、无真实设备模拟和嵌入式中控屏的基础数据对接；
- 断网或暂时没有后端时，用本地完整 JSON 保持可运行状态。

它**不是**以下系统的现成解决方案：

- 多个 App 之间的实时同步总线；
- 多进程、多用户同时写入的共享数据库；
- 高频采样、毫秒级控制、音视频或运动控制链路；
- 跨设备分布式一致性、消息队列、事件溯源或离线冲突合并系统；
- 替代正式后端 API、设备协议、数据库或 IPC 的通用中间件。

### 多 App 同步为什么不直接共用这两份 JSON

如果未来有多个 App（例如中控主屏、手机 App、维护 App、后台服务）同时读写同一目录，
仅靠两个 JSON 文件无法自然解决以下问题：

- 多个写入方同时替换同一文件时的顺序、覆盖和冲突；
- 一个 App 写入后，其他 App 如何即时获知变更；
- 控制命令的去重、幂等、回执关联和权限校验；
- 多设备离线后重新上线时，哪一份状态才是最终状态；
- 文件锁、进程崩溃、机器断电和跨平台 rename 语义差异。

因此，多 App 场景不能简单地让每个 App 都直接编辑这两份文件。应升级为“单一数据服务/设备服务
拥有数据，多个 App 通过受控接口访问”的结构，例如：

```text
设备/后端
   -> 数据服务或设备服务（唯一写入协调者）
      ├─ 中控屏主控应用
      ├─ 手机/平板 App
      └─ 维护/后台工具
```

此时 JSON 可以继续作为单个 App 的缓存或故障快照，但不能再作为多 App 的最终一致性总线。
正式扩展时应增加 IPC、HTTP/WebSocket、消息队列或数据库，并定义版本号、时间戳、来源、命令 ID、
回执 ID、权限和冲突策略；这些内容不属于当前单 App JSON 契约。

### 非中控屏高速场景为什么不直接复用

当前运行链路以约 2 秒一次的 runtime 轮询为基线，重点是中控屏上“数据可读、状态稳定、异常可恢复”，
不是追求极限实时性。对于非中控屏的高速场景，例如电机闭环、快速告警、连续波形、毫秒级采样或
大量设备同时上报，文件轮询会带来明显限制：

- JSON 序列化和完整校验会产生不必要的 CPU、内存和存储开销；
- 每次原子替换都可能产生写放大和调度延迟；
- 轮询只能发现“当前快照”，无法天然表达每一条事件的顺序；
- 2 秒周期无法保证快速控制和告警时延；
- 多个高速生产者写同一文件时，容易出现覆盖和丢事件。

高速场景应在底层采用实时任务、内存环形缓冲、事件队列、共享内存、专用 IPC 或设备协议，
由数据服务把适合 UI 的低频聚合快照再提供给中控屏。中控屏仍可读取本方案的 runtime 快照，
但不能把它当成高速控制链路本身。

### 当前方案的定位结论

可以把当前设计理解为：

> **面向单个中控屏主控应用的、低频运行快照 + 控制确认文件链路。**

它首先解决单 App 的职责分离、可离线联调、可恢复启动和控制结果确认；当产品扩展到多 App、
多进程或高速实时设备后，应保留字段语义和成功/失败边界，但把文件读写下沉为服务接口，
不要继续堆叠更多 JSON 字段来硬撑新的并发场景。

## 2. 一句话原则

**设置文件保存“已经确认的控制状态”，运行文件保存“外部产生的最新运行快照”；两者分开，
应用启动时都读，运行中只轮询运行文件，控制动作只写设置文件，界面只接受读回确认后的状态。**

这样拆分是为了避免以下问题：

- 传感器刷新覆盖用户刚刚确认的开关状态；
- 后端写运行值时误改 UI 的控制确认值；
- 一个大文件被两方同时改写，出现半写入或字段互相覆盖；
- 设备操作失败，但界面先乐观显示“已开启”；
- 旧版本遗留字段不断累积，升级后无法判断字段含义。

## 3. 文件与所有权

| 文件 | 业务含义 | 当前主要写入方 | 当前读取方 | 运行中读取策略 |
|---|---|---|---|---|
| `dashboard_settings.json` | 用户控制后已确认的总开关和五个设备开关 | 主控应用控制流程；未来由底层确认流程写入 | 主控应用主页 | 启动读取；控制写入后立即读回；不参加 2 秒轮询 |
| `dashboard_runtime.json` | 外部设备/模拟器产生的环境、天气、趋势和提示快照 | 当前可人工/模拟器更新；未来由底层数据源更新 | 主控应用主页、历史趋势页 | 启动读取；运行中每 2 秒读取一次 |

### 3.1 绝对禁止的写入方式

- 不要把两份文件合并为一个文件；
- 不要用 `dashboard_runtime.json` 保存用户开关；
- 不要用 `dashboard_settings.json` 保存温度、天气或趋势数组；
- 不要在运行中让外部程序持续改写 `dashboard_settings.json`，除非它就是控制确认链路；
- 不要直接截断原文件再写入；必须写临时文件，完成后原子替换；
- 不要添加未纳入当前 Schema 的扩展字段。字段需要扩展时先升级版本和契约，再同步应用。

## 4. 文件位置与落盘要求

应用默认把两份文件放在平台应用文档目录；构建/联调时可以用
`DASHBOARD_CONFIG_ROOT=/绝对路径` 指定目录。macOS 工程的固定联调目录是：

```text
/Users/as/Desktop/docu/workspace/beiang/flutter_home_dashboard_practice/config/
```

生产平台不要硬编码 macOS 路径，应使用平台应用文档目录或构建时明确指定的可写目录。
实际路径见 `BUILD_MACOS.md` 和 `BUILD_PLATFORMS.md`。

合作方更新文件时采用以下落盘步骤：

1. 在同一目录创建临时文件，例如 `dashboard_runtime.json.<时间戳>.tmp`；
2. 写入完整 JSON 文本并关闭文件；
3. 可选地重新读取临时文件做 JSON 解析和字段校验；
4. 使用同一文件系统内的原子 rename/replace 覆盖目标文件；
5. 不留下半写入的目标文件；失败时保留旧目标文件，等待下一次完整更新。

应用自身的控制保存也使用临时文件后 rename。读取方因此只会看到旧的完整文件或新的完整文件。

## 5. `dashboard_settings.json` 设置契约

### 5.1 完整结构

```json
{
  "settingsSchemaVersion": 1,
  "allDevicesOn": false,
  "airConditionerEnabled": false,
  "floorHeatEnabled": false,
  "freshAirEnabled": false,
  "humidifierEnabled": false,
  "pureEnabled": false
}
```

| 字段 | 类型 | 取值 | 含义 |
|---|---|---|---|
| `settingsSchemaVersion` | int | 当前为 `1` | 设置文件版本；不等于当前版本时整份重建 |
| `allDevicesOn` | bool | `true/false` | 主页一键开关的确认状态 |
| `airConditionerEnabled` | bool | `true/false` | 空调确认状态 |
| `floorHeatEnabled` | bool | `true/false` | 地暖确认状态 |
| `freshAirEnabled` | bool | `true/false` | 新风确认状态 |
| `humidifierEnabled` | bool | `true/false` | 调湿确认状态 |
| `pureEnabled` | bool | `true/false` | 超净确认状态 |

当前版本不在设置文件保存模式名称、温度设定值、风量、湿度目标值或设备通讯回执明细。
这些字段若以后成为正式产品需求，应先设计新的版本契约，不能由合作方自行塞进现有文件。

### 5.2 设置文件的状态语义

文件中的 `true` 不是“按钮刚刚被点击”，而是“控制流程已经确认的当前状态”。
当前无真实设备时，确认方式是：主控应用写入后读回内容正确；未来有真实设备时，确认方式应改为：

```text
用户点击
  -> 控制层生成一次控制意图
  -> 底层向真实设备下发
  -> 等待设备成功/失败/超时结果
  -> 成功才把实际状态写入 dashboard_settings.json
  -> 主控应用读回 settings 成功
  -> UI 更新样式并解除该按钮的处理中状态
```

失败、超时、读回失败都不能把 UI 改成目标状态；页面保留上一次已确认状态，并结束本次处理中状态，
等待用户下一次操作。

## 6. `dashboard_runtime.json` 运行契约

### 6.1 基本字段

| 字段 | 类型/单位 | 约束或含义 |
|---|---|---|
| `runtimeSchemaVersion` | int | 当前为 `8`；版本不符整份重建 |
| `areaName` | string | 非空；区域展示名，只读 |
| `outdoorTemperatureC` | int / ℃ | `-60..80`；当前主页室外温度读数 |
| `outdoorHumidityPercent` | int / % | `0..100` |
| `outdoorPm25` | int / µg/m³ | `0..1000` |
| `indoorPm25` | int / µg/m³ | `0..1000` |
| `indoorTemperatureC` | int / ℃ | `-20..60` |
| `indoorHumidityPercent` | int / % | `0..100` |
| `indoorCo2Ppm` | int / ppm | `0..10000` |
| `indoorFormaldehydeMgM3` | number / mg/m³ | `0..10` |
| `runningDeviceCount` | int | `0..totalDeviceCount` |
| `totalDeviceCount` | int | `1..1000` |
| `weatherCode` | string | 必须是工程支持的天气码；小写下划线命名 |
| `weatherTemperatureRange` | string | 非空；天气卡片下方范围文案 |

`weatherCode` 同时决定天气名称和静态 PNG/GIF 素材的映射；天气温度范围是独立字段。
例如 `wifiConnected=false` 时隐藏天气图、天气名称和气温范围，但室内温湿度/PM2.5 卡片仍保留。

### 6.2 连接、通知和提示字段

| 字段 | 类型 | 含义 |
|---|---|---|
| `wifiConnected` | bool | `false` 显示 Wi-Fi 错误图标，并隐藏天气区域的图、名称和范围 |
| `notificationBellVisible` | bool | 只控制铃铛本体；不推导红点 |
| `notificationBadgeVisible` | bool | 只控制红点/数字是否存在 |
| `notificationBadgeCount` | int | `0..9999`；为 0 时可显示红点，较大值 UI 显示为 `99+` |
| `noticeTriangleVisible` | bool | 只控制铃铛左侧告警三角形；不控制铃铛和红点 |
| `homeError` | string 或省略 | 非空时显示主页错误覆盖层；无错误时省略，不要写空字符串 |

铃铛、红点/数字、告警三角是三个独立控件。合作方不能根据“有通知”自动推导其他两个控件。

### 6.3 月趋势读取条件

| 字段 | 类型 | 约束 |
|---|---|---|
| `monthlyTrendYear` | int | `2000..2100` |
| `monthlyTrendMonth` | int | `1..12` |

月标题和二月截取规则由这两个字段决定。二月无论底层给 29、30 还是 31 项，UI 只读取前 25 项；
其他月份按数组实际长度读取，长度只能是 29、30、31，并以实际长度显示最后横轴刻度。

## 7. 趋势数组契约

温度、湿度和 PM2.5 各有室内/室外两组；CO₂ 当前只有室内一组。数组内元素均为有限 number，
不能有 `null`、字符串、`NaN` 或无穷值。

| 周期 | 数量 | 温度 | 湿度 | PM2.5 | CO₂ |
|---|---:|---|---|---|---|
| 日 | 12 | `dailyTemperatureIndoorC` / `dailyTemperatureOutdoorC` | `dailyHumidityIndoorPercent` / `dailyHumidityOutdoorPercent` | `dailyPm25Indoor` / `dailyPm25Outdoor` | `dailyCo2Ppm` |
| 周 | 7 | `weeklyTemperatureIndoorC` / `weeklyTemperatureOutdoorC` | `weeklyHumidityIndoorPercent` / `weeklyHumidityOutdoorPercent` | `weeklyPm25Indoor` / `weeklyPm25Outdoor` | `weeklyCo2Ppm` |
| 月 | 29/30/31 | `monthlyTemperatureIndoorC` / `monthlyTemperatureOutdoorC` | `monthlyHumidityIndoorPercent` / `monthlyHumidityOutdoorPercent` | `monthlyPm25Indoor` / `monthlyPm25Outdoor` | `monthlyCo2Ppm` |

单位和范围：温度 `-20..60 ℃`，湿度 `0..100 %`，PM2.5 `0..1000 µg/m³`，CO₂ `0..10000 ppm`。
月周期的七条数组必须同长。平均值不写入 JSON，由当前页面按实际读取数据计算，并四舍五入为整数。
因此合作方只负责提供原始采样，不要额外写入 `average`、`mean` 等字段。

隐藏趋势系列不是 runtime 字段：页面代码根据业务需要传入 `showIndoor/showOutdoor`。
隐藏的系列不会创建图例、平均值或曲线路径；CO₂ 当前默认只显示室内，未来可在代码层切换。

## 8. 应用生命周期与时序

### 8.1 首次启动

```text
定位应用文档目录
  -> 读取 settings
  -> 读取 runtime
  -> 文件不存在：用当前默认对象创建完整文件
  -> JSON 解析失败 / 缺字段 / 未知字段 / 类型错误 / 值越界 / 版本错误：
       整份对应文件重建为默认文件
  -> 合并两份有效数据
  -> 首屏显示
```

设置和运行文件分别校验、分别恢复；一份坏了不会阻止另一份读取。当前默认设备状态全部关闭，
`runningDeviceCount` 默认值为 `0`，避免没有真实设备时误显示运行中。

### 8.2 正常运行轮询

- 页面启动完成后，每 2 秒只读取 `dashboard_runtime.json`；
- 不在这条轮询里读取或覆盖 `dashboard_settings.json`；
- 上一次读取尚未结束时跳过下一次，避免慢读任务堆积；
- 读取并通过完整校验后，只替换当前数据的 runtime 部分，保留已经确认的 settings；
- runtime 无变化时不刷屏；变化日志最多每 30 秒一条；异常重建时立即记录一次；
- runtime 变化后，主页读数、天气、提示和对应趋势曲线更新；趋势曲线按当前页面的平滑动画过渡。

这也是拆分文件的核心收益：外部刷新运行数据不会覆盖用户刚确认的控制状态。

### 8.3 用户控制

```text
点击设备/一键开关
  -> 该控件进入处理中；连续点击无效
  -> 当前模拟阶段：写 settings 临时文件并原子替换
  -> 读回 settings
  -> 读回完整且值符合预期：更新 UI、解除处理中
  -> 写入/读回失败：保持旧 UI 状态、解除处理中并记录失败
```

运行中的手工修改 `dashboard_settings.json` 不会被 2 秒轮询主动发现；只有启动和控制写回会读它。
这是刻意的边界，不是漏读。

## 9. 四种数据链路情景

下面把合作时最容易混淆的四种情景拆开说明。前两种是当前 macOS 联调/无真实设备阶段，
后两种是接入正式设备后的目标链路。四种情景都坚持同一条边界：**settings 管确认状态，runtime
管外部运行快照；界面不把两个文件混读成一个写入通道。**

### 情景一：无真实设备，应用启动和首次建档

适用场景：当前 macOS 联调、客户没有接入 RS485/正式设备、首次运行或用户删除了两份 JSON。

```text
主控应用启动
  -> 定位应用文档目录/联调 config 目录
  -> 读取 dashboard_settings.json
  -> 读取 dashboard_runtime.json
  -> 分别执行当前 Schema 的完整校验
       ├─ 文件不存在：生成该文件的默认完整内容
       ├─ JSON 损坏：该文件整体恢复默认
       ├─ 缺字段/未知字段：该文件整体恢复默认
       └─ 类型/范围/版本错误：该文件整体恢复默认
  -> 合并一份有效 settings + 一份有效 runtime
  -> 首屏显示
```

无真实设备时的默认结果：

- `dashboard_settings.json` 的总开关和五个设备开关全部为 `false`；
- `dashboard_runtime.json` 使用工程默认的环境、天气、趋势和提示数据；
- `runningDeviceCount` 默认为 `0`，不会因为“没有设备”误显示设备运行；
- `areaName` 等 runtime 字段仍可显示默认值，它们是模拟快照，不代表真实测量；
- 两个文件分别恢复，一份损坏不会阻止另一份读取。

这一阶段没有“设备读取”动作。JSON 本身就是模拟数据源和持久化联调层，合作方可以直接准备
完整文件，或让应用第一次启动自动创建默认文件。

### 情景二：无真实设备，正常运行和用户控制

适用场景：当前练习工程通过 JSON 模拟天气、传感器和开关，用户手工修改文件观察 UI。

#### 2.1 运行数据链路

```text
模拟器/人工联调程序
  -> 写完整 dashboard_runtime.json 临时文件
  -> 原子替换 dashboard_runtime.json
  -> 主控应用每 2 秒读取 runtime
  -> 完整校验通过
  -> 只替换页面 runtime 数据
  -> 更新温湿度、PM2.5、CO₂、天气、通知、趋势曲线
```

这里的 runtime 写入方可以是脚本、后端模拟器或人工编辑工具，但必须遵守原子替换。
主控应用不在这条轮询中读取 settings，也不把当前页面的设备开关写回 runtime。

如果 runtime 在读取时异常：

1. 应用把整份 runtime 重建成默认文件；
2. 当前页面使用重建后的默认运行快照；
3. settings 中已经确认的设备状态不被清除；
4. 不使用“半个旧文件 + 半个新文件”的混合结果。

#### 2.2 控制数据链路

```text
用户点击设备开关/一键开关
  -> 控件进入处理中，连续点击无效
  -> 主控应用生成新的完整 settings 快照
  -> 原子写入 dashboard_settings.json
  -> 主控应用立即读回 dashboard_settings.json
       ├─ 读回成功且值符合预期：更新 UI，解除处理中
       └─ 写入/读回失败：保留旧 UI 状态，解除处理中
```

无真实设备时，“写文件成功并读回正确”就是本阶段的模拟控制成功条件。用户手工直接改 settings
后，页面不会被 2 秒 runtime 轮询主动刷新；重新启动，或通过页面再次控制后，才会重新读取 settings。
这不是漏读，而是为了保持设置文件的单一控制边界。

### 情景三：有正式设备，应用启动和运行快照同步

适用场景：R818/正式板端已经接入设备，底层能够从设备或后端取得实际状态和环境数据。

正式设备接入后，主控应用仍不直接操作 RS485。启动前由设备层完成一次“设备实际状态准备”，
再让应用读取两个 JSON：

```text
设备层启动
  -> 读取/探测真实设备当前状态
  -> 生成设备确认状态
  -> 写入/更新 dashboard_settings.json
  -> 采集环境、天气、连接和告警快照
  -> 写入完整 dashboard_runtime.json

主控应用启动
  -> 读取 dashboard_settings.json
  -> 读取 dashboard_runtime.json
  -> 严格校验并显示
```

运行中分成两条互不覆盖的链路：

```text
真实设备/后端数据源
  ├─ 传感器、天气、连接、告警、趋势采样
  │    -> dashboard_runtime.json
  │    -> 主控应用每 2 秒读取
  │
  └─ 已确认的设备开关状态
       -> dashboard_settings.json
       -> 主控应用只在启动和控制读回时读取
```

正式设备阶段的重点：

- runtime 中的 `runningDeviceCount`、传感器值和告警应来自设备/后端实际结果，不由 UI 猜测；
- settings 中的布尔值表示“设备已经确认的实际状态”，不是下发意图；
- 设备层更新两个文件时仍分别原子替换，不能先写半份 settings 再写半份 runtime；
- 设备层若暂时无法获得运行快照，可以延迟或丢弃过时读请求，但不能用错误值覆盖有效快照；
- 设备层若发现 settings 文件不合法，应按当前 Schema 重建或拒绝该次同步，不能把未知字段原样
  带入下一版本。

当前工程的 settings Schema 没有“回执文本/错误码”字段。正式设备的成功、失败、超时应先由底层
控制事务判断；只有成功的实际状态才写入当前 settings。详细回执若以后需要持久化，必须先设计
新 Schema，不能临时添加 `ack`、`result` 等未知字段。

### 情景四：有正式设备，用户控制和设备回执

适用场景：用户在正式板端点击空调、地暖、新风、调湿、超净或一键开关。

```text
用户点击
  -> 主控应用锁定该控件，后续连按无效
  -> 形成一条控制意图（目标设备 + 目标状态）
  -> 交给底层设备控制层
  -> 底层按 RS485/设备协议发送并等待应答
       ├─ 成功
       │   -> 取得设备实际状态
       │   -> 更新 dashboard_settings.json
       │   -> 原子替换完成
       │   -> 主控应用立即读回 settings
       │   -> 读回正确后更新样式并解锁
       │
       └─ 失败/超时/读回失败
           -> 不写入目标成功状态
           -> 保留上一次 settings 和 UI 样式
           -> 记录错误/结果
           -> 解锁控件，允许下一次完整操作
```

#### 4.1 写优先和读写冲突

RS485 的“写优先”属于底层调度，不属于主控应用页面逻辑：

1. 同一设备的一次发送和应答等待必须是完整事务；
2. 正在处理控制写入时，底层不能让普通轮询读插入并破坏事务；
3. 写请求可以排队稍后执行，但不能静默丢失；
4. 读请求可以延迟、合并或丢弃旧请求，下一次读取以最新快照为准；
5. UI 不需要为了总线冲突改成 500 ms 高频轮询，也不需要自己判断串口占用；
6. UI 只依据控制事务最终写入并读回的 settings 结果解锁和改变样式。

#### 4.2 连续点击和失败后的恢复

- 第一次点击有效后，按钮立刻进入处理中；
- 处理中再次点击全部忽略，不创建第二条并发控制意图；
- 成功：以设备实际状态为准更新 settings 和页面；
- 失败或超时：页面保留旧状态，不把“目标状态”伪装成实际状态；
- 读回失败：即使设备可能已经成功，也不能让 UI 擅自更新；应由底层补偿读取或等待下一次明确
  的确认结果；
- 事务结束后解除按钮锁定，下一次点击重新形成一条独立控制事务。

#### 4.3 runtime 与控制写入同时发生

正式设备运行时，runtime 可能持续被传感器链路更新，而 settings 可能被控制链路更新。
两者使用不同文件，应用层天然不会把两次写入合并到同一份 JSON：

```text
传感器链路 -> dashboard_runtime.json  -> 2 秒读取 -> 页面运行数据
控制链路   -> dashboard_settings.json -> 成功读回   -> 页面控制状态
```

底层仍须保证每个文件内部的原子替换；但 UI 不需要在两个 JSON 之间做锁，也不需要把 runtime 的
设备计数反向覆盖 settings 的五个开关。设备实际状态最终通过正确的写入方落到正确的文件。

## 10. 四种情景对照表

| 情景 | 运行快照写入方 | 设置写入方 | 主控应用何时读 runtime | 主控应用何时读 settings | 成功依据 |
|---|---|---|---|---|---|
| 无设备·启动 | 默认生成/模拟器 | 默认生成 | 启动时 | 启动时 | 两份文件完整有效 |
| 无设备·运行控制 | 模拟器/人工联调 | 主控应用 | 启动 + 每 2 秒 | 启动 + 控制写回 | 文件写入后读回正确 |
| 正式设备·启动运行 | 设备层/后端 | 设备确认链路 | 启动 + 每 2 秒 | 启动时 | 设备层已同步实际状态，文件通过校验 |
| 正式设备·控制 | 设备层/后端 | 设备成功后 | 启动 + 每 2 秒 | 启动 + 控制成功后读回 | 设备应答成功且 settings 读回正确 |

四种情景都不改变文件命名和字段含义；变化的是“谁产生数据”和“什么叫控制成功”。

## 11. 未来真实设备接入的合作边界

主控应用页面不直接感知 RS485，也不在本工程定义串口 API。合作方应把底层设备层放在应用外侧，
通过文件结果与 UI 对接：

1. 设备层接收控制意图，按设备协议完成发送、等待应答和必要重试；
2. 每一次控制必须返回明确的成功、失败或超时，不能静默丢弃；
3. 同一总线的读写事务由底层串行化；写优先，写请求不能丢，读请求允许延迟、合并或丢弃过时快照；
4. 只有真实设备成功后，设备层才把“实际状态 + 回执结果”落到设置确认链路，随后主控应用读回；
5. 失败或超时不写入目标成功状态，UI 继续显示上一次确认状态；
6. 设备实时测量、天气、网络和告警仍进入 `dashboard_runtime.json`，由 2 秒轮询显示；
7. 设备层不要为了处理 RS485 冲突而要求 UI 改成高频轮询，调度责任在底层。

简化后的分层关系：

```text
真实设备/模拟器
  ├─ 控制确认链路 ──> dashboard_settings.json ──(启动/控制读回)──> 中控屏界面
  └─ 运行快照链路 ──> dashboard_runtime.json  ──(启动/每2秒)──> 中控屏界面
```

## 12. 异常、升级和回退

当前策略是“整份文件严格校验，整份文件整体恢复”，而不是逐字段拼接：

- 根节点不是 JSON 对象：整份恢复；
- 文件缺字段、出现未知字段：整份恢复；
- Schema 版本不匹配：整份恢复；
- 类型错误、数值越界、趋势数量错误、天气码不支持：整份恢复；
- 月趋势七组数组长度不一致：整份恢复；
- 写临时文件或 rename 失败：保留旧目标文件，不用半写入内容刷新 UI；
- 运行文件异常：恢复 runtime 默认值，settings 仍独立保留；
- 设置文件异常：恢复 settings 默认值，runtime 仍独立保留。

这样做是为了升级后一次性丢弃不再认识的旧字段，避免多轮版本迁移留下不可追溯的累积状态。
如果未来必须兼容旧版本，应在明确的 Schema 迁移程序中完成迁移，再输出当前完整 Schema，
不要在页面读取时临时兼容一堆历史字段。

## 13. 合作方接入验收清单

接入前请逐项确认：

- [ ] 只使用两份文件名，没有创建或依赖 `dashboard_data.json`；
- [ ] 能生成完整 settings/runtime，不缺字段、不带未知字段；
- [ ] 所有外部写入均为“临时文件写完后原子替换”；
- [ ] runtime 每次更新保留完整趋势数组和正确的年月条件；
- [ ] 按月七组数组长度一致，且为 29、30 或 31；二月数据允许由 UI 截取前 25 项；
- [ ] 天气码为工程支持的小写下划线名称；不把天气温度范围硬编码到天气码中；
- [ ] Wi-Fi、铃铛、红点/数字、告警三角分别提供，不互相推导；
- [ ] 控制动作有成功/失败/超时结果；失败不会把 UI 伪装成成功；
- [ ] 无真实设备时，默认所有设备关闭；
- [ ] 人工修改 runtime 后，最迟约 2 秒能在页面看到变化；
- [ ] 人工修改 settings 后，理解它不会被运行轮询自动刷新；
- [ ] 用损坏 JSON、缺字段、未知字段、错误类型和越界值分别验证整份恢复；
- [ ] 在目标平台确认应用文档目录可写，并记录实际绝对路径；
- [ ] Linux、Windows、R818 的真实路径、权限和构建结果单独记录，不能用 macOS 结果代替。

## 14. 当前不在本契约内的内容

以下内容当前仍是后续开发，不要在合作接口中假设已经存在：

- RS485/Modbus/CAN 的具体帧格式、地址和重试次数；
- 温控模式、风量、目标温度等更细的设备设置字段；
- 多语言、主题持久化和运行时翻译资源；
- 正式后端鉴权、网络同步和离线冲突合并；
- 复杂历史数据存储、数据库索引和长期归档策略。

这些需求确定后，应先更新本文件的版本和字段表，再同步 Dart 模型、校验逻辑、示例 JSON 和测试。
